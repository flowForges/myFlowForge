node_modules/
dist/
out/
.DS_Store
# stale e2e artifacts written to a literal "~" dir in repo root
~/
docs
release/
# superpowers brainstorm/plan artifacts — kept local, never published
.superpowers/
# codex pet-build run artifacts (intermediate frames / 3d-import scratch) — never publish
.codex-pet-runs/

# 宠物内容全部移到 flowForges/pet-packs(见那边的 README)。应用仓库不再放任何宠物图片:
# 安装包只带一个内置 SVG 形象,其余按需下载。原稿 + 做包工具也在那边 —— 它们曾占本仓库历史的 85%。
# (make-growth-placeholder.mjs 是例外:纯脚本无图片,且 growthPlaceholder.integration.test 要跑它。)
assets/pet-packs/
scripts/growth_pets/
deliverables/

# ── macOS 签名 / 公证：绝不入库 ──────────────────────────────────────────────────────
# .signing.local.sh 里只该放 APPLE_SIGN_IDENTITY / FORGE_NOTARY_PROFILE 两个**非密钥**变量
# (真正的 App 专用密码存在 macOS 钥匙串里，见 scripts/macSigning.cjs 顶部)。即便如此也不入库:
# 它带着账号信息，而且总有人会往里塞密码。
.signing.local.sh
# 证书私钥。导出备份请放到**仓库外**(密码管理器 / 加密盘)。这行是最后一道网,不是第一道 ——
# 私钥一旦进了 git 历史,就算再删 commit 也等于泄露了,只能去苹果后台吊销证书重来。
*.p12
*.p8
*.cer
*.certSigningRequest
*.mobileprovision

# ── 安卓签名密钥:绝不入库 ────────────────────────────────────────────────────────────
# ★★★安卓密钥比苹果证书还要命:苹果那张丢了能吊销重发,**安卓这个丢了就永远发不了更新** ——
# 签名决定 app 的身份,换一个密钥等于换一个 app,老用户必须卸载重装、数据全丢。
# 所以它存在仓库外(~/keys/),密码存在 ~/.gradle/gradle.properties(也在仓库外)。
# 这几行是最后一道网,不是第一道。
*.keystore
*.jks
# gradle 的本地属性(sdk.dir / 签名口令都可能落在这里)
mobile/android/local.properties
